# Examples Index

The runnable E v0.1 example is the module demo:

```bash
python3 -m tfda_context_gate.e_observability.demo \
  --log-path /tmp/tfda-e-demo.jsonl
```

The A demo remains `python3 -m tfda_context_gate.a_router.demo --guard regex`.
These examples demonstrate individual boundaries; they are not an end-to-end
clinical workflow.


# Fixtures Index

The current B-to-C fixtures are generated or stored under `results/` and
historical `runs/*/results/`, with case definitions in
`c_generator/experiment_cases.py` and `hard_experiment_cases.py`.

They are intentionally not moved in this pass because their provenance points
to specific B run directories. A future canonical fixture package should retain
those provenance fields and add contract tests.

